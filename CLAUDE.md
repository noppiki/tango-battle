# 英検準2級 単語バトル — リファクタリング引き継ぎ

## プロジェクト概要
親子(小5・英検準2級対策)で遊ぶ単語学習ゲーム。現在 `eiken_pre2_game.html` 1ファイル(約55KB)に全て入っている。claude.ai(Web/モバイル)とローカルブラウザの両方で動くことが目標。

## 今回のタスク
1. **単語データの外部JSON化**: `<script>`内の `const W=[...]`(933エントリ、形式 `["word","意味","pos"]`、pos: v/n/a/d/c/j)を `data/words.json` に分離。fetchで読み込み。`file://` 直開きでfetchが失敗する環境向けに、`data/words.js`(`window.WORDS=[...]`)への自動フォールバックか、ビルド時インライン化のどちらかを用意する。
2. **モジュール分割**(ES modules推奨、ビルドなしで動く構成が望ましい):
   - `js/srs.js` — Leitnerボックス、重み付き出題(weight関数)、進捗ストレージ
   - `js/storage.js` — ストレージアダプタ(重要、下記参照)
   - `js/battle.js` — 対戦進行、スコア、スティール、ミラクル、サドンデス
   - `js/items.js` — アイテムボックス(7種、順位別テーブル、連続抑制)
   - `js/balance.js` — バランス定数を1箇所に集約(下記の定数表)
   - `js/audio.js` — 読み上げ(speechSynthesis)+ SD効果音(WebAudio)
   - `js/ui.js` — 画面描画
3. リファクタ後、既存の挙動が変わっていないことを目視+シミュレーションで確認。

## 壊してはいけない設計(重要な不変条件)
1. **正解判定は `button.textContent === 正解文字列`**。ゲッソー(イカスミ)の `inkify()` はspanで文字を「CSSで隠す」だけで textContent を変えない前提。innerHTML化する改修をするなら判定方法ごと見直すこと。
2. **RSスナップショット方式**: 追い上げ判定(おいつきボーナス、動的倍率、スティール許可、ミラクル発動)は全て「ラウンド頭(こども手番の前)に凍結した、ハンデ点を除いた実力スコア `RS`」で行う。生スコアで判定すると後攻(おうち)が構造的に有利になるバグが再発する(検証済み)。ミラクルの払い出しだけは生スコア差+20。
3. **交互パリティ**: `turn === qi % 2` が常に成立。再出題(+4位置挿入)・サドンデス(2問push)もパリティを維持している。挿入ロジックを触るときは要注意。
4. **`window.storage` は claude.ai Artifacts専用API**。ローカルでは存在しない(現状はメモリのみにフォールバック=保存されない)。`storage.js` アダプタで `window.storage → localStorage → メモリ` の3段フォールバックにするのが今回の改善ポイント(ローカルでも進捗が残るようになる)。キー: `eiken-p2-progress-v1`。
5. 効果音のAudioContextは必ずユーザー操作後に生成(自動再生制限)。
6. 進捗データ構造: `{child:{word:{b,t,w}}, parent:{...}, _m:{ls:{child,parent}, xp:{child,parent}}}`。後方互換を保つこと。

## バランス定数(balance.jsに集約すべき値)
- 出題数: 選択式10/16/20問(既定16)。ファイナルは常に最後の2問/ 再出題挿入位置: +4 / ファイナル: 最後の2問(対戦)x3
- 連続ボーナス: 3連続で+10 / アイテムボックス: 2連続ごと(所持1個まで)
- おいつきボーナス(RS差): 20点差+10 / 40点差+20
- 動的倍率抽選(RS差): 通常12%でx2 / 20点差50%でx2 / 40点差40%x3・35%x2
- スティール: リード中(RS基準)は不可 / 負け側の成功+10 / ミラクル失敗時は2倍で例外許可
- ミラクル: RS差20以上で発動可、1試合1回、成功=生差+20点、失敗=相手スティール2倍
- まけこしハンデ: 連敗×15点スタート(上限45)。**RSはハンデ除外で計算**
- アイテムテーブル: items.js内の3段テーブル(g<15 / ≥15 / ≥40)+直前アイテム重み1/4
- タイマー: ハンデ8000ms / サンダー5000ms
- XP: 正解10 / SD正解20 / スティール15 / ミラクル+30 / アイテム使用5。level=1+floor(sqrt(xp/60))

## 検証
`sim/balance_sim.js` に対戦バランスのモンテカルロ検証スクリプトあり(node実行)。リファクタ後にロジックを移植して回帰確認する場合の期待値:
- 55%/85%実力+連敗ハンデ定常: こども勝率 約34%
- 等実力75%: 先攻勝率 約46-48%(45%を大きく割ったらパリティバグを疑う)

## 備考
- 単語データはCEFR-J Wordlist(東京外国語大学投野由紀夫研究室)等を参考に独自編集。配布時はクレジット表記を推奨。
- 同義語ペア(illness/disease等)が存在するため、誤答選択肢の生成では「意味トークン(・区切り、括弧除去)が重複する語を除外」している。この除外ロジックも維持すること。

## 将来の配布形態を見据えた設計指針(PWA + Capacitor)
このゲームは (1) PWAとしてWeb公開 → (2) 同一コードをCapacitorで包んでApp Store配信、の二段構えを予定している。リファクタ時に以下を守ると移行コストが最小になる:
1. **ブラウザAPIへの直接依存を薄く**: `speechSynthesis` / `AudioContext` / ストレージ / 単語データの取得は、必ず各モジュール内のアダプタ関数経由で呼ぶ(例: `audio.speak(word)`, `store.get(key)`)。Capacitor移行時にネイティブプラグイン(TTS等)へ差し替える箇所を1ファイルに閉じ込めるため。
2. **`window.storage`(claude.ai専用API)を前提にしない**: storage.jsの3段フォールバック(window.storage → localStorage → メモリ)がその布石。Capacitor環境ではlocalStorage(またはPreferencesプラグイン)が本命になる。
3. **PWA対応をリファクタに含める**: `manifest.json`(name/short_name/icons/display:standalone/start_url/scope/theme_color)と最小限のService Worker(アプリシェル+data/words.jsonのキャッシュ、オフラインで全機能動作)を追加する。iOSはホーム画面追加後のみstandalone動作になる点に注意。
4. **相対パスで完結させる**: アセット参照は相対パスのみ(CDNフォントは失敗時フォールバック)。Capacitorのローカルファイル配信でもCloudflare Pagesでも同一ビルドが動くように。
5. **リリース時の名称注意**: 「英検」は日本英語検定協会の登録商標。アプリ名・manifest名には使わず、「準2級レベル」等の表現+非関係の注記をUIフッターに置けるようにしておく。
